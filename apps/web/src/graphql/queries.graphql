query companies {
  companies {
    id
    name
  }
}

query Company($id: ID!) {
  company(id: $id) {
    name
    characters {
      id
    }
  }
}

query CompanyMembers($id: ID!) {
  company(id: $id) {
    characters {
      id
      pseudo
    }
  }
}
